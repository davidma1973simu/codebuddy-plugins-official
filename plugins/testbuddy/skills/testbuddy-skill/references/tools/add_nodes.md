# add_nodes

添加节点到测试设计。**本文件仅负责场景判断和路由转发**，具体操作逻辑由对应场景的文档定义。

## 场景判断

通过 session 中的 `env` 字段判断当前运行环境，选择不同的添加方式：

## 路由规则

| 环境     | 判断条件             | 执行文档                                            |
| -------- | -------------------- | --------------------------------------------------- |
| IDE 环境 | `env == "codebuddy"` | 读取 `references/tools/codebuddy/add_nodes.md` 执行 |
